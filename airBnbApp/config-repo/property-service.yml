server:
  port: 8083

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/property_service_db
    username: ${SPRING_DATASOURCE_USERNAME:root}
    password: ${SPRING_DATASOURCE_PASSWORD:root}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jwt:
  secretKey: ${JWT_SECRET_KEY:mE+rFLi/VKK35G5GN08F59yCuFnFKvhg/axAzqLAJbw=}

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER_URL:http://localhost:8761/eureka/}
